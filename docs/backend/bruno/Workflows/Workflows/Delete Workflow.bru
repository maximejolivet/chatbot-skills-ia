meta {
  name: Delete Workflow
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/workflows/{{workflow_id}}
  body: none
  auth: basic
}

auth:basic {
  username: {{admin_username}}
  password: {{admin_password}}
}

docs {
  Soft delete (WorkflowSoftDeleteController) -- the row stays, it's excluded
  from active listings instead.
}
