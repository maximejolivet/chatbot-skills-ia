# Environment variables (.env.$APP_ENV.local covers any env: dev, prod,
# test, staging... rather than enumerating specific names)
.env
.env.local
.env.*.local

# Bruno environment files hold real credentials (admin password) -- request
# definitions in bruno/*.bru stay committed, only environments/*.bru (the
# actual values) are local. `**/` so it still matches wherever the bruno/
# collection itself lives in the tree (currently docs/backend/bruno/).
**/bruno/environments/

# Symfony
var/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn-integrity

# Frontend build outputs
dist/
build/
.next/
.nuxt/
.output/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log

# Docker
.dockerignore

# Temporary files
*.tmp
*.temp
